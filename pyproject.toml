[project]
name = "twfvconformal"
version = "0.1.0"
description = "Subject-specific time-warped B-spline factor variance model"
authors = [{ name = "Auto-generated by ChatGPT" }]
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["torch>=2.0"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

